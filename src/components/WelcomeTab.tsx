import IconImage from "./IconImage";

export default function WelcomeTab() {
  return (
    <div className="h-screen flex items-center justify-center">
      <IconImage src={`/icons/vscode.svg`} classname="size-64" />
    </div>
  );
}
